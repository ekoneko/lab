{
  "name": "test-service-worker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "tsc && cd code && static-server",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "static-server": "^2.0.5",
    "typescript": "^2.5.2"
  }
}
